# LCD 16x2 (I2C backpack PCF8574)

## Wiring to Arduino (Firmata)

| Backpack pin | Arduino |
|--------------|---------|
| VCC          | 5V      |
| GND          | GND     |
| SDA          | A4      |
| SCL          | A5      |

Default address is 0x27 (some modules use 0x3F: `$board->lcd1602(0x3F)`).
If the screen is blank — turn the contrast potentiometer on the backpack.

## Code

```php
$lcd = $board->lcd1602();
$lcd->write('Hello');
$lcd->setCursor(0, 1);
$lcd->write('femus!');
```

## Verification

`php examples/lcd-clock.php <port>` — first line shows "femus", second line shows ticking time.
